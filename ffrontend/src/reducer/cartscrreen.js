import Reactfr from "react";
function CartScreen(props){
    const productid={props.match.params.id:
const qty = props.location.search? Number(props.location.search.split("="))}
useeffectt {(=>
    if ({productid})
    {
        dispatch(addtocart{product, qty});
    })}
    functionCartScreen(props){
        return <div>
            cart screen
        </div>
    }
}