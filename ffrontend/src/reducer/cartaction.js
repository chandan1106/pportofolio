import { productlistreducer } from "./product"

const addtocart(productID, qty)=> {dispatch}=>
{
    try{
        const{data}=await axios.get("/api/products/"+producttID):dispatch({type:CARTADD_ITEM, and payload
        -id : data .id,
    dta.name,
product-ImageBitmapRenderingContext,
product-productlistreducer,
countstock,
qty
});
    }
}